DROP POLICY IF EXISTS "Signed-in users can add venue pins" ON public.venue_pins;
DROP POLICY IF EXISTS "Signed-in users can move venue pins" ON public.venue_pins;
DROP POLICY IF EXISTS "Signed-in users can edit venue plans" ON public.venue_plans;
DROP POLICY IF EXISTS "Signed-in users can add venue plans" ON public.venue_plans;