{
  "$schema": "visual-regression-library",
  "note": "Whitelisted overlap cases. Use bare `variantId` for all breakpoints or `variantId@mobile|tablet|desktop` to scope. Keep empty to enforce zero overlaps.",
  "allowedOverlaps": []
}
